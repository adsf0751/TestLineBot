# 專案協作規範

## 專案用途

本專案專屬於 LINE 遊戲服務，所有功能設計都應以 LINE Bot 的互動流程、訊息格式與使用情境為核心。

## 遊戲資料夾控管

各遊戲必須放在 `games/` 底下各自獨立的資料夾中，例如數字拼圖放在 `games/number_puzzle/`。新增遊戲時，不應把遊戲邏輯直接塞進 `app.py`；`app.py` 應維持 LINE Webhook、指令分流與回覆傳送的入口職責。

## 文件語言鐵則

之後所有文件皆須以自然、流暢的繁體中文撰寫。這是本專案的鐵則，任何新增或修改的文件內容都必須遵守。
